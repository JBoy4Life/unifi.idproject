import React from 'react'

import { Button } from '../../../elements'

const SiteWizzardUpload = props => (
  <div>
    <h2>Upload plan TBD</h2>
    <Button onClick={props.onSubmit} type="primary">Proceed</Button>
  </div>
)

export default SiteWizzardUpload
